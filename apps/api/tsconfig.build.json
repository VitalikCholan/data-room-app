{
  "extends": "./tsconfig.json",
  "comment": "prisma.config.ts is excluded deliberately: it is read by the Prisma CLI at its own TS entrypoint, never imported by the app. Compiling it puts apps/api at the tsc common root, which emits dist/src/main.js instead of dist/main.js and breaks every start command.",
  "exclude": ["node_modules", "test", "dist", "**/*spec.ts", "prisma.config.ts"]
}
